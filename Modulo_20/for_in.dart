main() {
  var notas = [8.9, 9.3, 7.8, 6.9, 9.1];

  /*
   * o for in (cmomo e conhecido) e o tipico foreach
   * no for in basicamente você define uma variavel para recerber
   * o valor da variavel na posição que esta o loop de repetição
   * 
   * 
   */
  for (var nt in notas) {
    print("Nota: $nt");
  }
}
